import React, { Component } from 'react';
import { Text } from "react-native"

export default class Menu_Berita extends Component {
    render(){
        return(
            <Text style={{textAlign : 'center'}}>
                Menu Berita
            </Text>
        )
    }
}