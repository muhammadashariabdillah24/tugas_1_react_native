import React, { Component } from 'react';
import { Text } from "react-native"

export default class Menu_Game extends Component {
    render(){
        return(
            <Text style={{textAlign : 'center'}}>
                Menu Game
            </Text>
        )
    }
}