import React, { Component } from 'react';
import { Text } from "react-native"

export default class Menu_Hits extends Component {
    render(){
        return(
            <Text style={{textAlign : 'center'}}>
            Menu Hits
            </Text>
        )
    }
}
