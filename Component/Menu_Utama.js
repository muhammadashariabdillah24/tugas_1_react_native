import React, { Component } from 'react';
import { Text } from "react-native"

export default class Menu_Utama extends Component {
    render(){
        return(
            <Text style={{textAlign : 'center'}}>
                Menu Utama
            </Text>
        )
    }
}